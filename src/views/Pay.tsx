import React from 'react';

import { Main } from '../containers';
import { Logo } from '../components';

const Pay: React.FC = () => {

  return (
    <div>
      <Logo />
      <Main />
    </div>
  );
};

export default Pay;
